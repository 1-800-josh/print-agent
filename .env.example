# Print Agent Configuration
#
# Configuration is now file-based. Copy config.example.json to config.json
# and edit the values there. Use --config flag or PRINT_AGENT_CONFIG env var
# to specify a custom config file path.

# Optional: Path to config.json file (for bootstrap only)
# PRINT_AGENT_CONFIG=config.json
